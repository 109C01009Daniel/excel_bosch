# 如何開啟和測試 Excel 搜索系統

## 📁 已索引的 6 個檔案

### 檔案列表

1. **Start of the art evaluation_Hibiscus2.xlsx** (12.73 KB, 18 單元格)
   - 風險評估相關

2. **Hibiscus II - 3 year demand MKP estimation v.2.xlsx** (16.59 KB, 42 單元格)
   - 3年需求預估

3. **Sample quantity_ Hibiscus 2_20251121.xlsx** (15.80 KB, 59 單元格)
   - 樣品數量

4. **5.12 Grand Master Test ODM Plan Hibiscus 2 V1.1.xlsx** (1054.38 KB, 922 單元格)
   - 測試計劃（最大的檔案）

5. **5.18 8D Report & Problem Solving Sheet.xlsx** (874.55 KB, 773 單元格)
   - 8D 報告

6. **5.25 Template Driver_Verification_List .xlsx** (207.26 KB, 774 單元格)
   - Driver 驗證清單

---

## 🧪 測試方法

### 方法 1：用命令列測試（推薦）

#### 步驟 1：查看統計資訊
```bash
python3 excel_search_cli.py stats
```

**預期輸出：**
```
📁 索引檔案數:     6
📝 總單元格數:     2,588
💾 檔案總大小:     2.13 MB
🗄️  資料庫大小:     1.0 MB
```

---

#### 步驟 2：測試搜索功能

**測試 A：搜索 "test" 關鍵詞**
```bash
python3 excel_search_cli.py search "test"
```

**測試 B：搜索 "Hibiscus"**
```bash
python3 excel_search_cli.py search "Hibiscus" --limit 5
```

**測試 C：搜索 "Driver" 並顯示完整行**
```bash
python3 excel_search_cli.py search "Driver" --full-row --limit 3
```

**測試 D：搜索 "product"**
```bash
python3 excel_search_cli.py search "product" --full-row
```

---

#### 步驟 3：執行快速測試腳本

我已經為你準備了一個自動測試腳本：

```bash
./quick_test.sh
```

這個腳本會自動執行上述所有測試。

---

### 方法 2：用 Excel 軟體開啟原始檔案

#### 開啟第 1 個檔案（最小的）：
```bash
# Linux (用 LibreOffice)
libreoffice "Sharepoint/Hibiscus2 - Documents/01_Project_Management/1.08_Milestones/01_QG0/1170_State of the Art, documented/Start of the art evaluation_Hibiscus2.xlsx"

# 或直接用檔案管理器打開
nautilus "Sharepoint/Hibiscus2 - Documents/01_Project_Management/1.08_Milestones/01_QG0/1170_State of the Art, documented/"
```

#### 開啟第 4 個檔案（最大的，測試計劃）：
```bash
libreoffice "Sharepoint/Hibiscus2 - Documents/06_Purchasing/6.09_Awarding Documents/01_RFQ/5.12 Grand Master Test ODM Plan Hibiscus 2 V1.1.xlsx"
```

---

### 方法 3：用 Python 直接讀取內容

```bash
python3 << 'EOF'
from openpyxl import load_workbook

# 開啟第一個檔案
file_path = "Sharepoint/Hibiscus2 - Documents/01_Project_Management/1.08_Milestones/01_QG0/1170_State of the Art, documented/Start of the art evaluation_Hibiscus2.xlsx"

wb = load_workbook(file_path, read_only=True, data_only=True)
sheet = wb[wb.sheetnames[0]]

print(f"📊 工作表: {wb.sheetnames[0]}")
print(f"📝 前 10 個單元格內容：\n")

count = 0
for row in sheet.iter_rows():
    for cell in row:
        if cell.value and count < 10:
            print(f"  {cell.column_letter}{cell.row} = {cell.value}")
            count += 1
    if count >= 10:
        break

wb.close()
EOF
```

---

## 🎯 推薦的測試流程

### ✅ 完整測試步驟

```bash
# 1. 查看統計
python3 excel_search_cli.py stats

# 2. 搜索常見關鍵詞
python3 excel_search_cli.py search "test"
python3 excel_search_cli.py search "Hibiscus"
python3 excel_search_cli.py search "product"
python3 excel_search_cli.py search "USB"
python3 excel_search_cli.py search "audio"

# 3. 測試完整行顯示
python3 excel_search_cli.py search "Driver" --full-row

# 4. 測試限制結果數量
python3 excel_search_cli.py search "test" --limit 3

# 5. 查看系統資訊
python3 excel_search_cli.py info
```

---

## 🔍 測試用關鍵詞建議

基於這 6 個檔案，以下關鍵詞應該能找到結果：

| 關鍵詞 | 預期找到 | 來源檔案 |
|--------|---------|---------|
| `test` | ✅ 有 | 測試計劃檔案 |
| `Hibiscus` | ✅ 有 | 多個檔案 |
| `Driver` | ✅ 有 | Driver 驗證清單 |
| `product` | ✅ 有 | 風險評估 |
| `audio` | ✅ 有 | 風險評估 |
| `sensor` | ✅ 有 | 風險評估 |
| `Sample` | ✅ 有 | 樣品數量檔案 |
| `8D` | ✅ 有 | 8D 報告 |
| `Problem` | ✅ 有 | 8D 報告 |
| `USB` | ❓ 可能 | 測試相關 |

---

## 💡 實用測試範例

### 範例 1：找測試相關內容

```bash
python3 excel_search_cli.py search "test" --full-row --limit 5
```

**你會看到：**
- 哪些檔案包含 "test"
- 在哪個工作表
- 在哪個位置
- 完整行的內容

---

### 範例 2：找 Driver 驗證項目

```bash
python3 excel_search_cli.py search "Driver" --full-row
```

**你會看到：**
- Driver 驗證清單的內容
- 每一行的詳細資料

---

### 範例 3：找風險相關內容

```bash
python3 excel_search_cli.py search "risk"
```

---

## 🐛 故障排除

### 問題 1：找不到關鍵詞

**檢查步驟：**
```bash
# 1. 確認檔案已索引
python3 excel_search_cli.py stats

# 2. 確認關鍵詞拼寫（不區分大小寫）
python3 excel_search_cli.py search "TEST"  # 也能找到 "test"

# 3. 使用部分關鍵詞
python3 excel_search_cli.py search "Dri"   # 可以找到 "Driver"
```

---

### 問題 2：想看資料庫內容

```bash
# 查看資料庫檔案
ls -lh excel_search.db

# 用 SQLite 瀏覽器查看（如果有安裝）
sqlite3 excel_search.db
```

在 SQLite 中：
```sql
-- 查看所有表
.tables

-- 查看檔案記錄
SELECT * FROM files;

-- 查看前 10 個單元格
SELECT cell_location, value FROM cells LIMIT 10;
```

---

## 📊 檢查資料庫內容

想看資料庫裡實際存了什麼？

```bash
python3 << 'EOF'
import sqlite3

conn = sqlite3.connect('excel_search.db')
cursor = conn.cursor()

# 隨機抽取 20 個單元格內容
cursor.execute("""
    SELECT f.file_name, c.sheet_name, c.cell_location, c.value
    FROM cells c
    JOIN files f ON c.file_id = f.file_id
    ORDER BY RANDOM()
    LIMIT 20
""")

print("🎲 隨機抽取 20 個單元格內容：\n")
for name, sheet, loc, val in cursor.fetchall():
    print(f"📄 {name} > {sheet} > {loc}")
    print(f"   {val[:50]}")
    print()

conn.close()
EOF
```

---

## ✅ 確認測試成功

如果你能看到以下輸出，代表系統運作正常：

1. ✅ `stats` 命令顯示 6 個檔案
2. ✅ `search "test"` 能找到結果
3. ✅ `search "Hibiscus"` 能找到結果
4. ✅ `--full-row` 能顯示完整行
5. ✅ 搜索速度 < 1 秒

---

## 🚀 下一步

測試成功後，你可以：

1. **索引更多檔案**
   ```bash
   python3 excel_search_cli.py index Sharepoint
   ```

2. **開始實際使用**
   - 搜索你需要的關鍵詞
   - 找測試用例
   - 找產品型號

3. **擴展功能**
   - 支援 .xls 格式
   - Web 介面
   - 更多搜索選項

---

**有問題隨時問我！** 🤝
